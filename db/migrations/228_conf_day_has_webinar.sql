-- 228. День программы: флаг «Имеет эфир/вебинар».
--
-- ЗАЧЕМ. В разделе «Вебинары» показывались ВСЕ дни события подряд, хотя эфир нужен
-- не в каждый день. Галочка «Имеет эфир/вебинар» у дня (для ЛЮБОГО типа события —
-- мероприятие/конференция/турнир): стоит → день в списке Вебинаров; нет → скрыт.
-- По умолчанию у нового дня — стоит (TRUE), чтобы не менять привычное поведение.

BEGIN;

ALTER TABLE conf_days
  ADD COLUMN IF NOT EXISTS has_webinar BOOLEAN NOT NULL DEFAULT TRUE;

GRANT SELECT, INSERT, UPDATE, DELETE ON conf_days TO plusson;

COMMIT;
