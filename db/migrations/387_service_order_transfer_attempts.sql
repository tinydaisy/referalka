-- 387. Счётчик попыток передачи прав на бота (09.09.2026).
--
-- ЗАЧЕМ.
-- Поллер повторял неудавшуюся передачу каждую минуту: на живом заказе вышла
-- 51 попытка подряд за час. Это долбёжка в BotFather с ЕДИНСТВЕННОГО
-- служебного аккаунта — ровно то, за что Telegram ограничивает аккаунты, а
-- потеря аккаунта означает остановку всей услуги.
--
-- Правило владельца: не получилось ДВА раза — задача закрывается с ошибкой, и
-- основателю уходит уведомление. Дальше разбирается человек, а не машина.
--
-- ⚠️ Считаем попытки в САМОМ ЗАКАЗЕ, а не в памяти процесса: поллер перезапускается
-- при каждом деплое, и счётчик в памяти обнулялся бы, начиная цикл заново.

BEGIN;

ALTER TABLE service_orders
    ADD COLUMN IF NOT EXISTS transfer_attempts INTEGER NOT NULL DEFAULT 0;

COMMENT ON COLUMN service_orders.transfer_attempts IS
    'Сколько раз пытались передать права. 2 неудачи → setup_state=failed и уведомление основателю';

COMMIT;
