-- 144: флаг "сотрудник/лидген" на уровне контакта.
-- Такой контакт:
--   • исключается из турнирной таблицы (не участвует как оцениваемый);
--   • в отчёте рефоводов его трафик суммируется в группу «Организатор»,
--     а сам он не показывается отдельной строкой среди рефоводов.
-- Глобально на контакт (у клиента сотрудник остаётся сотрудником во всех событиях).

ALTER TABLE contacts ADD COLUMN IF NOT EXISTS is_staff BOOLEAN NOT NULL DEFAULT FALSE;
