-- 408: Автонастройка Telegram — доступна ВСЕМ, без промокода.
--
-- Было: фича `tg_autosetup` только у `admin`, остальным услуга открывалась вводом
-- промокода в карточке услуги. Решение владельца 12.09.2026 — открыть всем
-- сразу, и новым, и уже существующим клиентам.
--
-- ⚠️ Код вводить больше не нужно НЕ потому, что ввод убрали, а потому, что
-- гейт (`_assert_feature`) теперь проходит у всех: экран с полем кода
-- показывается только тем, кому ручка ответила 403. Сам механизм промокода
-- оставлен рабочим — он ещё пригодится для других услуг.
--
-- ⚠️ Услуга БЕСПЛАТНА: `services.price = 0`, и при нулевой цене заказ сразу
-- создаётся оплаченным и уходит в очередь. То есть выдача фичи = выдача самой
-- услуги, отдельной оплаты не возникает.
--
-- ⚠️⚠️ ЛИМИТ BOTFATHER НИКУДА НЕ ДЕЛСЯ. Ботов создаёт ОДИН служебный аккаунт,
-- у @BotFather нарастающий лимит на `/newbot` (после десятка подряд — до 17
-- часов), а очередь считается по слотам (`tg_setup_accounts.max_slots`, по
-- умолчанию 5 непереданных ботов). Массовый наплыв не сломает аккаунт, но
-- люди будут ждать в очереди дольше — это ожидаемое поведение, а не сбой.

INSERT INTO tariff_features (tariff_id, feature_id)
SELECT t.id, f.id
  FROM tariffs t, features f
 WHERE f.slug = 'tg_autosetup'
ON CONFLICT DO NOTHING;
