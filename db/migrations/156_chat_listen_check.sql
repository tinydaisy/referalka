-- 156: хранение результата проверки «бот слушает чат» (для подсчёта заданий).
-- Чтобы статус в «Контроле заданий» не сбрасывался в «не проверено» после
-- перезагрузки страницы, а показывал «✓ проверено тогда-то».
--
-- Формат JSONB: { "telegram": {"ok": true, "at": "2026-06-17T15:00:00Z", "message": "..."},
--                 "vk": {...}, "max": {...} }

ALTER TABLE events
  ADD COLUMN IF NOT EXISTS chat_listen_check JSONB NOT NULL DEFAULT '{}'::jsonb;
