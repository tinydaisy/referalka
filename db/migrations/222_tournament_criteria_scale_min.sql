-- Минимальная оценка у критерия жюри.
-- Организатор может задать, что ниже этого балла ставить нельзя (напр. минимум 2).
-- Проверка работает и в кабинете жюри, и в дашборде.
-- Осмыслено только у критериев scorer='jury'; у остальных остаётся 0.
ALTER TABLE tournament_criteria
    ADD COLUMN IF NOT EXISTS scale_min NUMERIC NOT NULL DEFAULT 0;
