-- 256: Пункты меню на телефоне + сила свечения выделенного тарифа (240-255).
--
-- 1) nav_items получает признак `mobile` у каждого пункта (галочка в
--    конструкторе). Раньше на телефоне все пункты скопом уходили в
--    гамбургер — выбрать, что показать на узком экране, было нельзя.
--    Это правка ДАННЫХ внутри JSONB, отдельная колонка не нужна:
--    пункт без ключа `mobile` считается видимым (как было).
--
-- 2) featured_glow — насколько сильно подсвечивать выделенный тариф
--    (ширина ореола в px). Пусто → 24px, как было зашито в коде.

ALTER TABLE event_landing_blocks
  ADD COLUMN IF NOT EXISTS featured_glow INT;

GRANT SELECT, INSERT, UPDATE, DELETE ON event_landing_blocks TO plusson;
