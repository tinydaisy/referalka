-- 366: напоминание Instagram по умолчанию — через 10 минут, а не 3 часа
--
-- ⚠️ Три часа человек уже забыл, зачем писал комментарий: он листал ленту,
-- увидел рилс, написал слово — и ждёт материал сейчас, а не вечером.
-- Десять минут попадают в тот же сеанс, пока интерес живой.
--
-- ⚠️ Потолок остаётся 23 часа: Instagram разрешает писать только 24 часа с
-- последнего сообщения человека, за окном напоминание не уйдёт вовсе.
--
-- Существующим воронкам значение НЕ меняем: клиент мог выставить своё.

BEGIN;

ALTER TABLE instagram_funnels ALTER COLUMN reminder_delay_min SET DEFAULT 10;

COMMIT;
