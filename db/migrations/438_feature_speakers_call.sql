-- 438: Фича «Рассылка спикерам "вы следующие"» — пока только у админа.
--
-- Закрываем ФИЧЕЙ только сам шаблон speakers_call и его постановку в очередь.
-- Поля чата спикеров (events.*_speakers_chat_id/url) и ссылка входа спикера
-- (webinar_rooms.speaker_join_url) — доступны ВСЕМ, они гейтом не закрыты:
-- это обычные настройки события, полезные и без автоматической рассылки.
--
-- ⚠️ НАМЕРЕННО НЕ ПРИВЯЗЫВАЕМ НИ К ОДНОМУ ТАРИФУ. Пока обкатываем — фича
-- выдаётся точечно через админку (client_features), то есть по факту доступна
-- только там, где её включили руками. Когда решим открыть — добавляется
-- строка в tariff_features нужных тарифов, код при этом не меняется.

INSERT INTO features (slug, name, description, sort) VALUES
  ('speakers_call',
   'Рассылка спикерам «вы следующие»',
   'Автонапоминание в чат спикеров за 15 минут до выступления по программе: '
   'кто выходит, во сколько, ссылка входа в зум и кто готовится следом',
   205)
ON CONFLICT (slug) DO NOTHING;
