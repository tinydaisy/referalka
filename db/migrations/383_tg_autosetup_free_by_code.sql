-- 383. Автонастройка Telegram — бесплатно и по коду доступа (09.09.2026).
--
-- ЗАЧЕМ.
-- Услуга (миграция 364) стоила 790 ₽ и не продавалась вовсе: `coming_soon`
-- закрывал оплату, и подключить её можно было только строкой в базе руками.
-- На время обкатки владелец раздаёт услугу САМ — по кодовому слову, бесплатно:
-- очередь упирается в ЕДИНСТВЕННЫЙ сервисный аккаунт Telegram, и десяток
-- заказов подряд исчерпал бы его слоты. Код позволяет проверять услугу на
-- живых кабинетах, не открывая её всем.
--
-- ⚠️ ЦЕНА 0, А НЕ СКИДКА 100%.
-- Промокоды со скидкой (миграция 382) сюда не годятся: они про ДЕНЬГИ —
-- процент или рубли на заказ. Здесь денег нет вовсе, слово открывает ДОСТУП.
-- Скидка 100% на услугу ценой 0 ₽ — конструкция, которая ничего не означает
-- и всё равно требует заказа там, где заказа быть не должно.
--
-- ⚠️ САМО КОДОВОЕ СЛОВО ЖИВЁТ В КОДЕ (`ACCESS_CODE` в app/api/tg_autosetup.py),
-- а не в этой таблице. Оно временное — на недели обкатки — и меняется релизом.
-- Заводить под одно слово таблицу и экран управления значило бы построить то,
-- что придётся выбросить, когда услуга откроется всем.
--
-- ⚠️ `coming_soon` НЕ ТРОГАЕМ — он остаётся TRUE. Это и есть «продажи закрыты»:
-- кнопки оплаты нет ни на витрине, ни в разделе Каналов. Доступ выдаёт код.
-- Открыть продажу = снять галочку в админке, миграция для этого не нужна.

BEGIN;

UPDATE services
   SET price = 0,
       updated_at = NOW()
 WHERE slug = 'tg_autosetup';

-- ⚠️ Выданный по коду доступ живёт в `client_addons` с `source='promo'`
-- (значение уже разрешено ограничением таблицы — новых колонок не нужно).
-- Отдельного хранилища «кому открыт доступ» не заводим: о нём не знала бы
-- `client_has_feature`, и раздел остался бы закрытым, сколько ни выдавай.

COMMIT;
