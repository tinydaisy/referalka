-- 281: Картинка у вопроса анкеты (2026-08-12)
--
-- Своя картинка у КАЖДОГО вопроса: пример работы, схема, вариант дизайна —
-- вопрос «что вам ближе?» без изображения бессмысленен.
--
-- Порядок вопросов уже хранится в `sort_order` — миграция не нужна, меняем
-- только способ его задать (перетаскивание вместо ручного номера).
ALTER TABLE survey_questions
    ADD COLUMN IF NOT EXISTS image_url TEXT;

-- Обложка самой анкеты — показывается вверху, ДО вопросов (как на лендинге
-- клиента: сначала визуал, потом форма).
ALTER TABLE surveys
    ADD COLUMN IF NOT EXISTS image_url TEXT;
