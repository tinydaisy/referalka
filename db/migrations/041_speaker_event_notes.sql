-- ═══════════════════════════════════════════════════════════════════
-- Миграция 041: заметки спикера в событии
-- Поле notes в conf_speaker_events — произвольный текст для клиента
-- (например, частушки для ведущего, шпаргалка, контекст по гонорару).
-- Видно только в дашборде клиента, в публичные API не отдаётся.
-- ═══════════════════════════════════════════════════════════════════

ALTER TABLE conf_speaker_events
    ADD COLUMN IF NOT EXISTS notes TEXT;
