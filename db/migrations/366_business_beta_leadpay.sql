-- 366: Карточки LeadPay для «Бизнес Beta» + включение тарифа
--
-- Миграция 365 завела тариф СКРЫТЫМ: без карточек оплата невозможна (цена лежит
-- в карточке LeadPay, а не в запросе — см. tariff_periods.py), а кнопка
-- «Оплатить» на странице подписки активна всегда и `payable` не проверяет.
-- Карточки заведены владельцем — включаем.
--
--   период      сумма        карточка
--   30 дней      4 900 ₽     66204
--   180 дней    25 872 ₽     66203
--   360 дней    47 040 ₽     66202
--
-- ⚠️ Все три карточки проверены запросом `getLink` на боевых ключах: отвечают
-- `status: success` и отдают ссылку на оплату. Саму сумму LeadPay в ответе НЕ
-- возвращает — она задана внутри карточки в их кабинете, сверить её программно
-- нечем. Поэтому первую оплату каждого периода стоит глазами проверить на
-- странице LeadPay: сумма там обязана совпасть с той, что показал кабинет.

UPDATE tariffs SET
    leadpay_product_id      = '66204',
    leadpay_product_id_6mo  = '66203',
    leadpay_product_id_12mo = '66202',
    is_active = TRUE
  WHERE slug = 'business_beta';
