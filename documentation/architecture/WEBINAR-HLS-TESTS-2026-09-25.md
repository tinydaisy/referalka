# Журнал: «чёрный экран в вебинарной комнате», 25.09.2026

Событие 89, день 2, поток `live/bf3327e51f0b77fc`. Все времена — UTC (МСК −3).

> ⚠️ Зачем этот файл. Без журнала я дважды записал в документацию как
> «действующее решение» то, что на самом деле сломалось, и **потерял, какая
> версия работала**. Здесь — что менял, что проверил и КАКОЙ БЫЛ ОТВЕТ, а не
> вывод по памяти.

## Симптом (со слов владельца)

| Устройство / браузер | Поведение |
|---|---|
| iPhone Safari | работает, но после F5 часто не грузит |
| iPhone Chrome | не работает |
| iPhone Яндекс | не работает («мелькнул и исчез») |
| Android MI Browser | не работает |
| Android Chrome | не работает |

«Работает только Safari» — устойчиво, на всех этапах.

## Найденная причина

MediaMTX v1.19 требует от плеера **cookie-сессию**:

```
GET /live/{key}/index.m3u8       → 302 + Set-Cookie: hlsSession=...
GET /live/{key}/main_stream.m3u8 → 401 «authentication error»   (без cookie)
```

Проверено: **401 стабильно, 5 запросов из 5.**

⚠️ **Поток при этом исправен:** `recording 2 tracks (H264, MPEG-4 Audio)`, запись пишется, муксер поднят. `ffprobe` по свежему сегменту — `h264` + `aac`, звук есть.

**Почему только Safari:** он играет HLS **нативно** (`<video src>`), cookie уходит сама и CORS его не касается. Все остальные идут через **hls.js → XHR**, а XHR cookie по умолчанию не посылает.

⚠️⚠️ **Ключевая деталь (найдена последней):** MediaMTX отдаёт cookie ДВАЖДЫ, второй раз с `Partitioned`:

```
set-cookie: cookieCheck=1
set-cookie: cookieCheck=1; HttpOnly; Secure; SameSite=None; Partitioned
```

`Partitioned` (CHIPS) — вот почему одного `withCredentials` не хватило для Chrome/Яндекса.

## Что пробовали: что именно и с каким результатом

| № | Правка | Результат | Действует? |
|---|---|---|---|
| 1 | Убрать дублирующий `add_header Access-Control-Allow-Origin "*"` из `location /live/`, `/hls/` | Нужна, но недостаточна | **ДА** |
| 2 | `xhrSetup: xhr.withCredentials = true` в hls.js | Safari + Chrome + MI пошли; **Яндекс нет** | **ДА** |
| 3 | `hlsTrustedProxies: [127.0.0.1/32]` | `child=401` — без изменений | нет, откачено |
| 4 | `hlsCDNSecret: ""` | `child=401` — без изменений | нет, откачено |
| 5 | `hlsDirectory` + `root` в nginx (отдача файлами) | **ПОЛОЖИЛО ВСЁ, включая Safari** | нет, откачено |
| 6 | `playerLoading` стартует в `true` (крутилка сразу) | Убрало «чёрный экран без кнопки» | **ДА** |

### Почему №1 нужна

MediaMTX сам отдаёт CORS с конкретным origin + `Allow-Credentials: true`. Наш `add_header` приходил ВТОРЫМ — браузер отбрасывает ответ с двумя разными CORS-заголовками, а с cookie значение `*` запрещено стандартом. ⚠️ Вернуть = снова чёрный экран, даже с №2.

### Почему №5 всё положило (главный урок)

`hlsDirectory` **затёрся при откате `mediamtx.yml` из бэкапа**. MediaMTX перестал писать сегменты, а СТАРЫЕ плейлисты остались лежать на диске. nginx отдавал их с кодом **200**, плеер шёл за сегментами → **404**.

```
04:50:03  main_stream.m3u8            200  761      ← плейлист есть
04:50:03  6335cde59c4d_main_seg194.ts 404  564      ← а сегмента нет
```

⚠️⚠️ **У схемы ДВЕ части** (`root` в nginx + `hlsDirectory` в mediamtx.yml). Отвалилась вторая — первая **не падает, а отдаёт мусор с кодом 200**: ломается МОЛЧА и в опасную сторону. Проксирование так деградировать не может.

**Если возвращаться к №5** — только вне эфира и обязательно проверив, что сегменты РЕАЛЬНО пишутся:

```bash
find /var/www/plusson/media-server/hlsout -name '*.ts' | wc -l   # должно быть > 0
```

Наличия `.m3u8` НЕДОСТАТОЧНО — именно на этом и погорели.

## Гипотезы, которые проверка ОПРОВЕРГЛА

| Гипотеза | Проверка | Итог |
|---|---|---|
| Плохие телефоны / слабый интернет / VPN | логи: поток исправен, 401 на сервере | **опровергнута** |
| Проблема в кодировщике | `recording 2 tracks`, ffprobe: h264+aac | **опровергнута** |
| Старый JS в кеше браузера | все 4 браузера грузили актуальный `page-13432001c0ed8d28.js` | **опровергнута** |
| MediaMTX не отдаёт `Allow-Credentials` на 302 | `grep -ic` по ответу = **1**, заголовок есть | **опровергнута** |

⚠️ Третья и четвёртая — мои ошибочные выводы, озвученные владельцу до проверки. Проверять ДО заявления, а не после.

## Ошибки процесса (мои)

1. ⚠️⚠️ **Дважды менял схему раздачи видео во время ЖИВОГО эфира**, опираясь на одну зелёную проверку. Откат занимал минуты, но зрители в это время не видели ничего.
2. ⚠️⚠️ **Перезапускал MediaMTX при идущем эфире** (04:38, 04:41) — это единственное, что рвёт поток. Обрывы потом искали как «опять пропало».
3. ⚠️ **Массовые 401 в 04:13–04:28 — след моих же рестартов**, не жалоба зрителей. Сверять время, прежде чем считать это симптомом.
4. ⚠️ **Не вёл этот журнал с начала** — записывал в документацию выводы, и дважды записал сломанное как «действующее». Потерял, какая версия работала.

## Что рвёт живой эфир, а что нет (проверено)

| Действие | Рвёт? |
|---|---|
| Сборка фронта, `deploy`, `deploygit` | **Нет** (проверено дважды на живом) |
| `systemctl reload nginx` | **Нет** |
| **`systemctl restart plusson-mediamtx`** | **ДА** |
| `restart plusson-api`, Celery | Видео нет; чат может мигнуть |

Эфир ради деплоя останавливать **не нужно**.

## Состояние на 05:05 UTC

- nginx: **проксирует** в `127.0.0.1:8888` (откат к рабочей схеме)
- Сборка на проде: `page-13432001c0ed8d28.js`, коммит `f7604d39`
- Ошибок за 3 минуты: **0** (только штатные 302 за cookie)
- Поток: `ready: true`, H264 + AAC
- **Итог для зрителя: Safari работает, остальные — нет.** Причина (`Partitioned` cookie) найдена, лечение не доделано.

## Что осталось сделать (ВНЕ эфира)

1. **Довести №5 до конца** — `hlsDirectory` + проверка `.ts > 0` + переключение nginx. Тогда cookie не нужна вовсе и разница между браузерами исчезает. Это лечение причины, а не симптома.
2. ⚠️ Разделять код по браузерам (запрос владельца) — **не рекомендую**: выбор всего один (нативный HLS для Safari / hls.js для остальных), а ветки под Chrome-iOS, Яндекс-iOS, Chrome-Android, MI пришлось бы вечно поддерживать, и каждая может отвалиться молча. №1 убирает саму причину и работает везде одинаково.
3. ⚠️ Отдельная болезнь, НЕ путать с cookie: `WAR segment duration changed from 2s to 4s` — нестабильный keyframe **у кодировщика**, рывки на iOS. Лечится в Zoom/OBS (интервал 2 с), не в коде.

## Как диагностировать в следующий раз

```bash
# коды по браузерам
grep '/live/' /var/log/nginx/access.log | tail -50
# 401 на main_stream.m3u8  → cookie-болезнь
# 404 на .ts при 200 на плейлисте → пуст каталог сегментов (см. №5)
# 200 на .ts → видео физически доехало

# какой чанк реально исполняет зритель
grep 'MiuiBrowser' /var/log/nginx/access.log | grep -oE 'page-[a-f0-9]+\.js' | sort -u
ls web/.next/static/chunks/app/webinar/\[slug\]/\[day\]/     # сверить с актуальным

# исправен ли поток
curl -s http://127.0.0.1:9997/v3/paths/list
```

⚠️ Смотреть **коды ответов**, а не слова зрителя: «не работает» одинаково звучит при 401, при 404 и при заблокированном автозапуске — а лечение у них разное.


---

# 26.09.2026 — второй день разбора, после провального вебинара

## Итог вебинара 25.09 (разбор по базе и логам)

| Показатель | Значение |
|---|---|
| Уникальных зрителей (`contact_id`) | **158** |
| Получили видео | **87** |
| **НЕ получили видео** | **71 (45%)** |

⚠️ Считать надо по `contact_id`, а НЕ по IP: по IP выходило 252/116/136 — один человек даёт несколько адресов (мобильная сеть, Wi-Fi + 4G, переподключения).

⚠️⚠️ **Цифра 71 ЗАНИЖАЕТ беду:** она построена на кодах ответов nginx, а «сегмент отдан 200» не значит «человек видит». Часть из 87 «получивших» тоже сидела с чёрным экраном.

**Пострадали ВСЕ связки, а не отдельные браузеры** (по IP, пропорции верны): Android Chrome 47, iOS Safari 28, Windows Chrome 19, Android Яндекс 12, Android MI 11, Windows Edge 8, Windows Firefox 5.

### Почему падало к концу эфира

| Час | Успешно | Ошибок 404 | Зрителей |
|---|---|---|---|
| 08:00 | 48 382 | 302 | 58 |
| 09:00 | 52 175 | 157 | 67 |
| 10:00 | 32 278 | 494 | 43 |
| 11:00 | 29 611 | 372 | 31 |
| **12:00** | 25 319 | **1626** | 28 |

Причина в логе MediaMTX:
```
08:01  segment duration changed from 2s to 3s - will cause an error in iOS clients
09:01  segment duration changed from 3s to 4s - will cause an error in iOS clients
```
Муксер сам растягивал сегменты, а при запасе 7 сегментов плеер не успевал забрать сегмент до удаления.

⚠️ **Zoom при этом исправен:** keyframe ровно 2,00–2,03 с (ffprobe по записи). Версия «виноват кодировщик / 60 fps» **опровергнута**: `avg_frame_rate` = 29,8, а `60/1` — лишь заявленный максимум. В Zoom править нечего.

⚠️ За эфир **266 сессий закрыто по неактивности** — дефолт `readTimeout` = 10 секунд.

## Что сделано 26.09

### 1. ЛОГ ФАКТА ВОСПРОИЗВЕДЕНИЯ (миграция 516) — главное

Раньше причину было НЕЧЕМ искать. Теперь:
```bash
journalctl -u plusson-api -f | grep WEBINAR-NOVIDEO
```
```
WEBINAR-NOVIDEO room=18 contact=15500 mode=hlsjs played=0s err=levelLoadTimeOut device=Mozilla/5.0 (Linux; Android 10; K)...
```
`played=0s` — не запустилось вовсе; больше нуля — шло и прервалось. `device` — полный User-Agent.

⚠️ Признак берётся по РОСТУ `video.currentTime`, а не по отсутствию ошибки: при чёрном экране события `error` часто нет вовсе.

### 2. Серверные параметры

| Параметр | Было | Стало | Зачем |
|---|---|---|---|
| `hlsSegmentCount` | 7 (14 с) | **15 (30 с)** | даже при растягивании до 4 с запас 60 с — 404 не будет |
| `readTimeout` | 10 с | **300 с** | сессия не закрывается, пока человек отвернулся |
| `hlsMuxerCloseAfter` | нет | **300 с** | поток не пересоздаётся при уходе зрителей |
| `hlsVariant` | mpegts | **mpegts** | оставлен |

⚠️⚠️ **`fmp4` ПРОБОВАЛИ И ОТКАЗАЛИСЬ.** Он разделяет видео и звук на отдельные дорожки (`EXT-X-MEDIA` + `video1_stream.m3u8` + `audio2_stream.m3u8`) и требует `EXT-X-VERSION:10`. Старые Android-плееры раздельные дорожки часто не собирают — а чёрный экран был именно на Android (MI 8, 2018 г.). `mpegts` даёт ОДНУ дорожку и `EXT-X-VERSION:3`.

⚠️⚠️ **Почему 15, а не 30.** 30 ставили 25.09: iOS Safari починился, но **сломались Android MI и iOS Яндекс**. Тогда причину не понимали и откатились на 7. Сегодня лог показал: дело было в `levelLoadTimeOut` — плейлист из 30 сегментов Android не успевал загрузить за дефолтные 10 секунд hls.js. 15 сегментов + явные таймауты решают обе задачи.

### 3. Таймауты hls.js — первая причина, найденная ПО ЛОГУ

Было задано число попыток, а сами таймауты — нет, работал дефолт 10 с.

| Таймаут | Было | Стало |
|---|---|---|
| `manifestLoadingTimeOut` | 10 с (дефолт) | **20 с** |
| `levelLoadingTimeOut` | 10 с (дефолт) | **20 с** |
| `fragLoadingTimeOut` | 10 с (дефолт) | **40 с** |

⚠️ Это и объясняло загадку «сегменты отдаются 200, а экран чёрный»: сервер отдавал, а плеер уже бросил попытку по таймауту.

### 4. iOS Яндекс — чёрный экран после возврата с другой вкладки

Уходишь на другую вкладку, возвращаешься — буфер обнулён, чёрный экран, **обновление страницы не помогает** (`video.src` уже стоит, повторного `load()` нет). В логе — `mode=native played=0s`.

Причина в `catchUp`: он выходил по `if (!v.buffered.length) return` — ровно в том случае, который надо лечить. Теперь при пустом буфере вызывается `reloadNative()`.

⚠️ Под hls.js в `src` не лезем: у него своя обработка обрыва, подмена источника рвала бы загрузку.

## Куда попадает каждый браузер (текущая логика)

Условие одно: `isIOS && /YaBrowser/` → нативный, **всё остальное** → hls.js.

| ОС + браузер | Путь |
|---|---|
| iOS Яндекс | нативный `<video>` |
| iOS Safari, iOS Chrome, iOS Firefox, iOS Opera | hls.js |
| Android (все браузеры) | hls.js |
| Windows / Mac (все браузеры) | hls.js |

## Результаты тестов 26.09 (на живом эфире, комната 18 / день 3)

| ОС + браузер | После таймаутов |
|---|---|
| iOS Safari | **работает** |
| iOS Chrome | **работает** |
| iOS Яндекс | **работает**, но чёрный экран после возврата с вкладки → правка №4 |
| Android MI | не работает → лечится таймаутами, перепроверить |
| Android Chrome | не работает, `err=levelLoadTimeOut` → лечится таймаутами, перепроверить |
| iOS Firefox | не работает → тот же hls.js, перепроверить |

## Главные уроки процесса

1. ⚠️⚠️ **Без лога факта воспроизведения причину найти НЕЛЬЗЯ.** Два дня ушло на перебор настроек, а первая настоящая причина (`levelLoadTimeOut`) нашлась за минуту, как только лог появился.
2. ⚠️⚠️ **«Сегменты 200» ≠ «человек видит».** Не докладывать «работает» по логам nginx.
3. ⚠️ **Не менять схему раздачи на живом эфире** — дважды сломал зрителям.
4. ⚠️ **Рестарт MediaMTX: Zoom НЕ переподключается сам** — предупреждать ДО.
5. ⚠️ **Разведение путей по браузерам — не лечение.** Проблема была серверная и общая; перебор «кому какой путь» лечил одних и ломал других.

## ТЕСТ 26.09.2026, сборка page-5a86cc336152d1c6 (таймауты hls.js)

| ОС + браузер | Результат |
|---|---|
| iOS Safari | **работает** |
| iOS Chrome | **работает** |
| iOS Яндекс | работает; после возврата с вкладки чёрный квадрат — 1-й раз вернулось, 2-й нет, тап не помог |
| Android Яндекс | работает; после возврата висит → тап → значок плей → грузится → **второй значок плей** |
| Android MI | картинка появилась, но **висит**; пошла только после множества нажатий |
| Android Chrome | **не работает**, в логе `err=levelLoadError` |

### Найдено по логу

**Android Chrome — cookie.** В nginx: **7 ошибок 401** на `main_stream.m3u8` против 6 успешных. MediaMTX ставит HLS-сессию как `SameSite=None; Partitioned` — атрибуты для МЕЖСАЙТОВЫХ вставок, а запрос у нас same-origin (`pluson.ru` → `pluson.ru`). Chrome на Android такие cookie в XHR не подставляет.

Лечение: `proxy_cookie_flags ~ nosecure samesite=lax;` в `location /live/`.
⚠️ `Partitioned` одной строкой остаётся (для его удаления нужен модуль `headers-more`, которого нет), но первая cookie идёт уже без него. Проверено: цепочка мастер → дочерний → сегмент = 200/200/200, причём **cookie-jar пуст** — доступ работает и без cookie.

**Два значка плей — один мой, второй браузерный.** Персиковый круг «Смотреть эфир» рисуем мы; второй значок Android-браузер рисует САМ поверх остановленного видео, это его встроенное поведение (у `<video>` нет `controls`).

⚠️⚠️ **Почему они жили не в лад:** моя кнопка показывалась только по `playerStuck`, а он ставился ЛИШЬ при ошибке плеера. Самый частый случай — «видео висит, ошибки нет»: тогда моей кнопки не было вовсе, зритель видел чёрный прямоугольник и догадаться ткнуть в экран было неоткуда.

Лечение: сторож по ФАКТУ — не растёт `currentTime` 6 секунд → показываем кнопку. Порог 6 с, а не 2: короткие остановки на мобильной сети — норма, кнопка на каждом джиттере мешала бы.

⚠️ Прошлый сторож (снятый 24.09) строился на `readyState` и давал петлю «нажал → буферизация → снова кнопка». Здесь петли нет: смотрим на рост времени, а `playerLoading` скрывает кнопку на время загрузки.

## ТЕСТ 26.09.2026, сборка page-59c5cc7285bde7cf

### Настройки этой сборки (полностью)

**mediamtx.yml:** `hlsVariant: mpegts`, `hlsSegmentCount: 15`, `hlsSegmentDuration: 2s`, `hlsAlwaysRemux: yes`, `readTimeout: 300s`, `hlsMuxerCloseAfter: 300s`, `hlsAllowOrigin: "*"`

**nginx `location /live/`:** проксирование в `127.0.0.1:8888`, `proxy_pass_header Set-Cookie`, `proxy_cookie_flags ~ nosecure samesite=lax`, БЕЗ `add_header Access-Control-Allow-Origin`

**hls.js:** `withCredentials: true`, таймауты `manifest/level 20 с`, `frag 40 с`, повторы 6/6/8, буферы 30/60, `liveSyncDurationCount: 3`, `liveMaxLatencyDurationCount: 10`

**Пути плеера:** `isIOS && /YaBrowser/` → нативный; всё остальное → hls.js

**Сторож «картинка висит»:** кнопка при отсутствии роста `currentTime` 6 с (в этой сборке — БЕЗ проверки флага загрузки, отсюда петля)

### Результаты

| ОС + браузер | Результат |
|---|---|
| iOS Safari | **работает** («если смотреть») |
| iOS Chrome | **работает** |
| iOS Яндекс | да, но картинка висит, мелькнула синяя кнопка плей и исчезла, **сильно лагает** |
| Android Chrome | сначала чёрный → чёрно-персиковый плей → **заработало** |
| Android MI | **не работает**: чёрный экран → кнопка без синего плей → нажатие → грузится → снова плей, по кругу |
| Android Яндекс | **зависло**: чёрный → плей → грузится → плей → и так по кругу |

### ⚠️⚠️ ГЛАВНОЕ: я СОЗДАЛ ПЕТЛЮ своим сторожем

Сторож ставил `playerStuck` → кнопка → нажатие → `reloadPlayer` → видео уходит в буферизацию → `currentTime` не растёт → **сторож снова поднимает кнопку**. Зритель физически не мог начать смотреть.

Это ТА ЖЕ петля, которую убирали 24.09 (тогда она строилась на `readyState`) — я вернул её другим способом.

**Лечение:** сторож молчит, пока идёт загрузка (`loadingRef.current`), порог поднят с 6 до 10 секунд.

⚠️ Флаг читается через **ref**, а не из состояния: хук с пустыми зависимостями замкнул бы первое значение и сторож не сработал бы никогда; а добавить в зависимости нельзя — интервал пересоздавался бы и сбрасывал счёт.

⚠️ Лечить таймаутом «подождать N секунд после нажатия» НЕЛЬЗЯ: на медленной сети буферизация дольше любого разумного N.

### Что в логе

```
iOS-Chrome      mode=hlsjs played=0s err=bufferStalledError   ×4
Android-Chrome  mode=hlsjs played=0s err=levelLoadError       ×1
Android-Chrome  mode=?     played=0s err=-                    ×2
```

`bufferStalledError` у iOS Chrome — буфер встал; совпадает с жалобой «сильно лагает».

### Синяя кнопка плей — браузерная, не наша

Наша кнопка — персиковый круг (`#FFCFA4`). Синяя/чёрная — рисует **сам браузер** поверх остановленного видео (у `<video>` нет `controls`, это встроенное поведение). Убрать её нельзя; наша задача — чтобы своя кнопка появлялась ВСЕГДА при остановке, тогда рассинхрона не будет.
