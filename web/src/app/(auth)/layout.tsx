/**
 * Общая обёртка страниц входа, регистрации и подтверждения почты.
 *
 * ⚠️⚠️ `force-dynamic` ОБЯЗАТЕЛЕН, иначе Next отдаёт эти страницы с
 * `Cache-Control: s-maxage=31536000` — то есть кеширует HTML на ГОД. После
 * каждой выкатки имена JS-файлов меняются, а браузер продолжает держать
 * вчерашний HTML со ссылками на файлы, которых уже нет. Скрипт не грузится,
 * и форма превращается в картинку: кнопка «Войти» нажимается, но НИЧЕГО не
 * происходит — ни запроса, ни ошибки на экране.
 *
 * Именно так это и выглядело у клиента 08.09.2026: в логах API его попыток
 * входа не было вовсе (ни 200, ни 401) — запрос до сервера не доходил.
 * Диагностировать тяжело: в инкогнито всё работает, а у человека «тишина».
 *
 * На корне сайта (`app/page.tsx`) `force-dynamic` уже стоял по этой же
 * причине — здесь его просто забыли.
 */
export const dynamic = 'force-dynamic'

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
