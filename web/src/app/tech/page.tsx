'use client'

/**
 * Мои клиенты — кабинет внедренца.
 *
 * ⚠️⚠️ ЭКРАН ОБЩИЙ С АДМИНКОЙ (`components/tech/ClientsScreen`, 23.09.2026).
 * Раньше здесь был свой запрос и своя таблица БЕЗ фильтров: у владельца в
 * «Клиентах» отбор по тарифу, подписке и боту, а у внедренца ничего. Две
 * таблицы одной сущности разъезжались и в колонках, и в правилах.
 *
 * Единственное отличие кабинетов: у владельца есть выбор внедренца, здесь его
 * номер подставляет сервер из токена — параметром он не принимается.
 *
 * ⚠️ ТОЛЬКО СМОТРИМ: менять тарифы, подключать модули и запрещать
 * коллаборации отсюда нельзя — это решения владельца.
 */
import { Suspense } from 'react'
import ClientsScreen from '@/components/tech/ClientsScreen'

export default function TechClientsPage() {
  return (
    <Suspense fallback={null}>
      <ClientsScreen mode="tech" />
    </Suspense>
  )
}
