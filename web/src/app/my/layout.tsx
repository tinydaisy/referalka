/**
 * Обёртка кабинета покупателя и партнёра (`/my`, продукт, урок).
 *
 * ⚠️⚠️ Существует РАДИ `force-dynamic`. Без него Next отдаёт страницы кабинета
 * с `Cache-Control: s-maxage=31536000` — кеширует HTML на год. После выкатки
 * имена JS-файлов меняются, браузер держит вчерашний HTML со ссылками на
 * несуществующие файлы, скрипт не грузится — и форма входа перестаёт работать
 * молча: кнопка нажимается, запроса нет, ошибки на экране нет (поймано у
 * клиента 08.09.2026, в логах API его попыток входа не было вовсе).
 *
 * ⚠️ Разметки тут нет намеренно: вся оболочка кабинета — в `CabinetShell`,
 * второй слой вложенности сломал бы полосу меню во всю высоту.
 */
export const dynamic = 'force-dynamic'

export default function MyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
