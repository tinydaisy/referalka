// Тонкая обёртка: нарезка записи у турнира — та же страница, что у конференции.
// Компонент сам определяет раздел по pathname (как карточка события).
export { default } from '../../../../conferences/[id]/recordings/[recId]/page'
