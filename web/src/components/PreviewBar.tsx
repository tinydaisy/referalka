/**
 * Плашка «вы смотрите предпросмотр» поверх черновика лендинга.
 *
 * ⚠️ Нужна обязательно. Предпросмотр открывает страницу по обычному адресу
 * (`/e/{slug}`, `/pr/{slug}`) — визуально он неотличим от живой страницы, и
 * без плашки клиент решит, что лендинг уже опубликован, а посетители всё это
 * время будут упираться в 404.
 *
 * Серверный компонент: ни состояния, ни обработчиков здесь нет.
 */
export default function PreviewBar() {
  return (
    <div
      className="sticky top-0 z-50 w-full px-4 py-2 text-center text-sm font-medium"
      style={{ background: '#FFCFA4', color: '#25455D' }}
    >
      Предпросмотр черновика — посетители эту страницу пока не видят.
      Опубликуйте её в кабинете, когда будете готовы.
    </div>
  )
}
