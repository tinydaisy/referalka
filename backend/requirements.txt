# ⚠️⚠️ Версии здесь ДОЛЖНЫ совпадать с тем, что реально стоит на проде.
# Файл полтора года жил своей жизнью: пакеты на сервере обновляли, сюда не
# записывали. Конфликт вылез 2026-09-07 при первой же переустановке —
# aiogram 3.4.1 требует pydantic<2.6, а закреплён был 2.6.4, и deploygit
# встал на ResolutionImpossible, не выкатив ничего.
#
# ⚠️ Правило: обновил пакет на сервере — впиши сюда. Проверить расхождение:
#   pip show fastapi pydantic aiogram | grep -E '^(Name|Version)'

fastapi==0.135.1
uvicorn[standard]==0.42.0
asyncpg==0.29.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
httpx==0.27.0
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1
aiogram==3.26.0
celery==5.3.6
redis==5.0.3
nanoid==2.0.0
boto3==1.34.84
Pillow==10.3.0
# Проверка DNS своих доменов клиентов (CNAME для лендингов, SPF/DKIM/DMARC для почты).
# Фактически уже стоял как транзитивная зависимость — фиксируем явно, чтобы не
# пропал при пересборке окружения (миграция 269).
dnspython==2.8.0

# Печать лендинга в PDF (см. app/services/landing_pdf.py): управляем headless
# Chrome по протоколу отладки. Фактически уже стоял транзитивно — фиксируем
# явно, чтобы не пропал при пересборке окружения.
websockets==16.0

# Автонастройка Telegram «под ключ» (миграция 364): работа от лица живого
# аккаунта по MTProto — только так можно говорить с @BotFather (создание бота,
# привязка Mini App, передача владения). Bot API этого не умеет в принципе.
# ⚠️ Версия та же, что в мейлере — на ней проверены наши сессии и прокси.
Telethon==1.43.2
# ⚠️⚠️ python-socks, а НЕ PySocks. Telethon 1.43 умеет прокси только через него:
# при одном PySocks он пишет «proxy argument will be ignored» и идёт НАПРЯМУЮ.
# Поймано на проде 07.09.2026: узбекский номер подключался с российского IP —
# верный путь к блокировке аккаунта, и молча, без единой ошибки.
python-socks[asyncio]==2.7.2
PySocks==1.7.1
# ⚠️ Проверка занятости имени бота ходит на t.me ЧЕРЕЗ ПРОКСИ: с российского
# IP Telegram отвечает через раз («Network is unreachable»). Без socksio httpx
# отказывается работать с socks5-прокси вовсе.
socksio==1.0.0
